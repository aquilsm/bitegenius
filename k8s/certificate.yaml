apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bitegenius-tls
  namespace: bitegenius
spec:
  secretName: bitegenius-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - bitegenius.masdev.online
